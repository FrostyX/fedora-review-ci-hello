# Running fedora-review on every commit

This is a fake Fedora DistGit repository for the `hello` package.
https://src.fedoraproject.org/rpms/hello

The hello packing is temporarily broken so we are using `light` instead.
https://src.fedoraproject.org/rpms/light

Run workflows locally with:

```
act -W .forgejo/workflows/
```
